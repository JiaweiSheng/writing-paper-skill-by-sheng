# Writing Paper by Sheng（中文说明）

> 本文是 [`SKILL.md`](SKILL.md) 的中文对照，供中文使用者阅读。Agent 实际执行仍以英文 `SKILL.md` 为准。

| 字段 | 内容 |
| --- | --- |
| 名称 | `writting-paper-skill-by-sheng` |
| 说明 | 按 Jiawei Sheng 的问题-机制-证据写法，起草或改写有证据支撑的英文计算机科研论文。用于摘要、引言、相关工作、方法、实验、结论、全文一致性，以及面向审稿人的改写；尤其适用于 AI、NLP、IR、知识图谱、推荐、图学习与多模态。触发词包括：写论文、润色论文、按我的风格写、paper writing。不要把科学审稿、文献发现、引用核验或投稿格式检查作为主任务。 |
| 许可证 | MIT |
| 兼容性 | 任何支持 Agent Skills 的 Agent，包括 Cursor、Codex、Claude Code |
| 版本 | 1.0.0 |
| 作者 | JiaweiSheng |

写出主张可追溯的论文：

```text
任务与设定 -> 观察到的局限 -> 因果机制 -> 设计原则
-> 命名后的方法模块 -> 检验每条主张的证据
```

参考论文只提供写法，不提供可复用内容。不要抄它们的句子、实体例子、类比、数字、引用或主张。优先保留用户的技术含义和术语，而不是模仿表面措辞。

## 先分流请求

先判断任务模式，只加载需要的指南：

- 风格模仿或生成散文：读 [style-and-discourse.md](style-and-discourse.md)
- 按节或全文起草：读 [section-moves.md](section-moves.md)
- 改写、润色、压缩或按意见修改：读 [revision-workflow.md](revision-workflow.md)
- 主张、证据、术语或跨节一致性：读 [evidence-and-consistency.md](evidence-and-consistency.md)
- 句子级用词：论证成立后再查 [phrase-bank.md](phrase-bank.md)，不要靠拼接短语成文
- 风格校准或示例：查 [examples.md](examples.md)
- 风格来源与把握程度：查 [sources.md](sources.md)

短段落只加载直接相关的文件。用户明确要求按 Sheng 的表达风格时，必须加载风格/话语指南。全文起草或大幅修改时，同时使用风格指南、章节指南和证据/一致性指南。

## 先订立写作契约

能从用户材料推断的就推断，只在缺项会实质改变文本时提问：

- 交付物和目标长度
- 会议或读者，若已知
- 任务、设定、输入、输出和评价单位
- 核心缺口及其原因
- 方法名、模块名和 intended insight
- 已有证据：核实过的事实、给定结果、计划实验、引用和未知项
- 改写自由度：只润色、可重组，或实质性重写

未指定会议时，写中性会议散文，不要声称具体格式要求。用户提供原文时，除非被要求重新考虑，否则保留所有有依据的技术主张。

## 核心决策

### 先立论，后润色

在内部用六个字段概括论文：**task, setting, gap, cause, insight, evidence**。若缺口和根因被混在一起，先把观察到的失败与被假设导致它的机制分开。只有当读者能看出该方法为何必要时，再引入方法。

对每条主要主张使用这条诊断链：

```text
难在何处？ -> 现有机制为何会产生它？
-> 解应具备什么性质？ -> 哪个模块制造该性质？
-> 哪项实验能证伪或支持该主张？
```

当工作只建立了经验关联时，不要强写成因果解释。因果用语必须匹配实际设计与证据。

### 改编常见写法，而不是套死模板

以下是特征默认项，不是普遍强制：

- 任务、失败模式或方法交互难以理解时，具体例子或图才有用；没有解释价值就省略
- 文献里确实存在范式时，按机制分组；只有自然支持两组时才分成两组，否则用最小可辩护分类
- 困难或诊断性设定应能检验中心机制；不要为了像某种风格而制造困难设定
- 紧凑摘要通常需要 6-8 个修辞动作，但句数和顺序取决于会议、篇幅和技术复杂度
- 贡献条数跟随真实贡献，通常 2-4 条；不要把一个想法拆成空子弹

### 主张强度不超过证据

- 不要编造引用、结果、数据统计、显著性、复杂度、消融或实现细节
- 缺失内容用有用的占位符标出，例如 `[RESULT NEEDED: metric, dataset, comparison]`、`[CITATION NEEDED]`、`[VERIFY: causal interpretation]`
- 区分已证明、被暗示和被设计的性质：直接证据用 `demonstrates`，间接证据用 `suggests`，尚无结果时用 `is designed to`
- `significant` 只留给已报告的统计检验；否则写测量差异，或在可辩护时使用 `substantial`
- 新颖性是比较出来的。写清在形式化或机制上相对哪个先验范式有何不同；避免无依据的 first/only 声称

## 风格画像

追求正式、密实、以机制为中心的英文。每段只做一件主事，常见推进是：

```text
主张或问题 -> 机制/理由 -> 后果/证据 -> 过渡
```

优先使用具体科学主语：任务、数据、表示、模块、约束或结果。作者选择用 `we`。`this paper` 少用。术语一次定义、一个标签，不要为了变化而换近义词。

可识别节奏是**受控对比后接建设性解决**：先写清现有方法做到了什么，再隔离剩下的问题，解释它为何还在，然后引入能解决它的最小设计。细节见 [style-and-discourse.md](style-and-discourse.md)。

避免宣传性语言、空洞的困难声称、引用堆砌、公式倾倒、无解释的结果，以及空泛过渡。不要继承源论文里的语法错误或过时惯例。

## 会议与领域适配

把会议惯例当作用户当前提供的约束，而不是永恒事实。当精确格式、匿名、页限或必需要节真正重要时，在本 Skill 之外核实。

- NLP 类论文常把 Related Work 靠前，并受益于精确任务定义、数据条件和错误类别
- Web/IR/推荐类论文常突出系统设定、对比范式、研究问题，以及稀疏或分布偏移下的行为
- 通用 AI 论文常需要更紧的动机和显式定义或目标，但不要添加没有推理作用的形式化
- 短文应保留主张链，同时压缩背景、分类和次要分析
- 离开源领域时，保留论证原则，但采用目标领域的术语、证据规范和章节惯例。不要导入知识图谱特有的例子或基准假设

## 输出行为

- 若要求起草：返回可投稿的正文，只保留必要占位符或假设
- 若要求修改：先返回改写文本。仅在有用或被要求时简列实质改动
- 若原文含糊：保留含义并标出歧义，不要悄悄裁定
- 若要求的主张超过证据：给出最强仍可支持的措辞，并指出何种证据才能支撑更强版本
- 匹配用户已有的 LaTeX、Markdown 或纯文本约定
- 除非引用工作明确在范围内，否则原样保留引用和标签

## 交稿前质量门

1. 读者能否识别 task、setting、gap、cause、insight、evidence？
2. 每个方法模块是否回答了此前提出的需求？
3. 每条中心贡献是否有对应评价，或有明确证据占位符？
4. 主张是否不强于已有结果和研究设计？
5. 术语、缩写、符号、模块名、数据集名和指标是否稳定？
6. Abstract、Introduction、Method、Experiments、Conclusion 是否讲同一个故事，且没有互相抄写？
7. 对已有工作的描述是否准确、尊重，没有稻草人化？
8. 每段是否只承担一个主要修辞功能？
9. 是否删除了无依据的最高级、含糊强调词和抄来的源论文细节？
10. 文本是否符合所要求的会议、篇幅、格式和改写幅度？
